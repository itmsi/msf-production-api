# Database Connection Mode
# Set to 'ON' to use server database via SSH tunnel
# Set to 'OFF' to use local database
DB_HIT_SERVER=OFF

# Server Configuration (used when DB_HIT_SERVER=ON)
SERVER_HOST=162.11.0.232
SERVER_USERNAME=msiserver
SERVER_PASSWORD=m0t0r519ht5!@#

# SSH Tunnel Configuration (used when DB_HIT_SERVER=ON)
SSH_HOST=162.11.0.232
SSH_PORT=22
SSH_USERNAME=msiserver
SSH_PASSWORD=m0t0r519ht5!@#
DB_HOST=127.0.0.1
DB_PORT=5432
LOCAL_TUNNEL_PORT=6543

# Database Configuration
# For SERVER database (when DB_HIT_SERVER=ON):
# POSTGRES_HOST=127.0.0.1 (tunnel endpoint)
# POSTGRES_PORT=6543 (tunnel port)
# POSTGRES_USER=sharedpg
# POSTGRES_PASSWORD=pgpass
# POSTGRES_DB=tid-project

# For LOCAL database (when DB_HIT_SERVER=OFF):
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=falaqmsi
POSTGRES_PASSWORD=your_local_password
POSTGRES_DB=msf_production

# Application Configuration
NODE_ENV=development
PORT=3000

# JWT Configuration
JWT_SECRET=your-jwt-secret-key-here
JWT_EXPIRES_IN=24h

# Other configurations
CORS_ORIGIN=*
